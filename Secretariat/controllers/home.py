"""Legacy home controller module.

Canonical route handlers for `/home`, `/schedule`, and `/availability`
are defined in `Secretariat.__init__.create_app`.

This file is intentionally kept as a marker to prevent duplicate route
implementations across multiple modules.
"""
